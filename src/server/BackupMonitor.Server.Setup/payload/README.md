# Server payload

`server-payload.zip` is generated by `scripts/build-turnkey.ps1` immediately before
the self-contained Server Setup publish. It contains the self-contained API,
database migrations, client download package, and a version-pinned PostgreSQL
Windows runtime with its license files.
