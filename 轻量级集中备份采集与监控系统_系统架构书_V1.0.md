# 轻量级集中备份采集与监控系统
# 系统架构书

**文档版本：** V1.0  
**编制日期：** 2026-07-31  
**适用范围：** 中小型企业 Windows 服务器环境  
**最低客户端系统：** Windows Server 2012 R2  
**部署模式：** 局域网独立部署  
**系统定位：** 应用备份文件集中采集、校验、留存、监控与恢复管理平台

---

## 1. 项目背景

企业现有多台 Windows 服务器，各业务应用已经具备自身的自动备份功能，例如：

- 用友 ERP 自动账套备份；
- SQL Server 数据库备份；
- OA、MES、业务管理系统自动备份；
- ZIP、RAR、BAK、DB、DAT 等单文件备份；
- 多种类型文件共同构成的一整套备份；
- 不同账套或业务单位分别存放在多个子目录中的备份；
- 以完整目录形式生成的备份。

目前这些应用备份文件分散在不同服务器、不同磁盘和不同路径中，需要管理员每周逐台登录服务器，人工判断最新备份并复制到局域网备份机。

现有方式存在以下问题：

1. 人工操作耗时，容易遗漏服务器、应用或账套；
2. 难以准确判断文件是否仍在生成；
3. 大文件复制中断后可能需要重新开始；
4. 缺乏统一的成功、失败和异常记录；
5. 无法及时发现应用本周未生成新备份；
6. 无法证明源文件与集中备份文件完全一致；
7. 公共 SMB 共享容易受到勒索病毒和凭据泄露影响；
8. 缺乏统一的版本保留和恢复管理；
9. 无法集中查看服务器资源和客户端运行状态。

因此，计划建设一套轻量级集中备份采集系统。

---

## 2. 系统建设目标

系统应实现以下核心目标：

1. 集中管理所有业务服务器及其应用备份任务；
2. 自动识别每个任务最新、完整、稳定的一份备份；
3. 支持单文件、目录、多文件组合和多账套备份；
4. 支持自动、人工确认、完全手动等多种上传模式；
5. 客户端主动通过局域网上传，不开放业务服务器入站端口；
6. 支持分块上传、断点续传、限速、错峰和并发控制；
7. 服务端对文件重新计算哈希并验证完整性；
8. 客户端不能覆盖或删除服务端已归档备份；
9. 集中展示客户端在线状态、CPU、内存、磁盘等基础状态；
10. 发现未生成备份、未上传、文件异常、磁盘不足等问题；
11. 备份文件保持原始目录和原始格式，脱离系统仍可使用；
12. 系统结构轻量，适合中小企业部署和维护。

---

## 3. 系统非目标

第一阶段不建设以下功能：

- 操作系统整机镜像；
- 裸机恢复；
- 虚拟机快照备份；
- 数据库在线备份引擎；
- 替代用友、SQL Server 等应用自身备份功能；
- 云盘或公网对象存储；
- 跨互联网数据传输；
- 大型日志分析平台；
- 全功能服务器性能监控；
- 任意 PowerShell 或 CMD 远程执行；
- 补丁管理和资产管理；
- 微服务集群；
- Kubernetes；
- 多租户 SaaS；
- 复杂全局文件去重。

系统只负责：

> 发现备份、判断完整性、集中采集、文件校验、版本留存、运行告警和恢复取回。

---

## 4. 设计原则

### 4.1 轻量优先

客户端只运行一个 Windows Service，不提供本地 Web 页面，不开放监听端口，不安装大型运行组件。

服务端采用模块化单体架构，不拆分微服务。

### 4.2 客户端主动连接

所有通信均由业务服务器上的客户端主动发起。

服务端不使用管理员共享、SMB、WinRM、远程注册表等方式进入业务服务器。

### 4.3 服务端集中控制

服务端统一管理：

- 客户端注册；
- 备份任务；
- 识别规则；
- 上传模式；
- 上传时间；
- 并发数量；
- 带宽限制；
- 版本保留；
- 告警规则。

### 4.4 权限最小化

客户端只读取指定备份目录，只能上传自己所属任务的数据，不能浏览、覆盖或删除服务端历史备份。

### 4.5 原始文件优先

备份文件按原始格式保存，不使用复杂的专用仓库格式。

系统停用后，管理员仍可直接从备份目录找到并使用原始文件。

### 4.6 备份结果可证明

每次备份必须有：

- 文件清单；
- 源端哈希；
- 服务端哈希；
- 文件数量；
- 文件大小；
- 发现时间；
- 上传时间；
- 入库时间；
- 执行结果。

### 4.7 复制成功不等于备份成功

系统必须分别判断：

1. 应用是否生成了新的备份；
2. 生成的备份是否完整；
3. 备份是否成功上传；
4. 服务端是否完成校验和入库；
5. 备份是否经过恢复验证。

---

## 5. 总体架构

系统采用“集中服务端 + 轻量客户端 Agent”的架构。

```text
┌───────────────────────────────────────────────────────┐
│                    管理员浏览器                        │
│             服务器管理、任务配置、人工下发             │
└───────────────────────┬───────────────────────────────┘
                        │ HTTPS 管理访问
                        ▼
┌───────────────────────────────────────────────────────┐
│                    集中备份服务端                      │
│                                                       │
│  管理控制台 │ 任务调度 │ 指令队列 │ 上传接收          │
│  文件校验   │ 状态监控 │ 告警服务 │ 审计日志          │
│  元数据库   │ 临时区   │ 正式备份仓库                 │
└───────────────▲──────────▲──────────▲─────────────────┘
                │          │          │
         HTTPS/mTLS  HTTPS/mTLS  HTTPS/mTLS
                │          │          │
┌───────────────┴─┐ ┌──────┴────────┐ ┌──────┴────────┐
│ ERP服务器Agent │ │ OA服务器Agent  │ │ MES服务器Agent │
│ 扫描本地目录   │ │ 扫描本地目录   │ │ 扫描本地目录   │
│ 判断完整备份   │ │ 判断完整备份   │ │ 判断完整备份   │
│ 主动上传       │ │ 主动上传       │ │ 主动上传       │
└─────────────────┘ └───────────────┘ └───────────────┘
```

---

## 6. 部署架构

### 6.1 客户端部署

每台业务服务器安装一个客户端 Agent。

支持系统：

- Windows Server 2012 R2；
- Windows Server 2016；
- Windows Server 2019；
- Windows Server 2022；
- 后续版本 Windows Server。

建议技术：

- .NET Framework 4.8 Windows Service，优先保证 2012 R2 兼容性；
- 或经过完整验证的自包含现代 .NET Windows Service。

客户端不依赖：

- IIS；
- SQL Server；
- Java；
- Docker；
- 本地浏览器；
- 本地 Web 服务。

### 6.2 服务端部署

服务端可部署在：

- Windows Server 2019/2022；
- Windows 11 Pro/Enterprise 专用电脑；
- 试点阶段可部署在普通 Windows 电脑。

正式环境建议使用专用设备，不建议与日常办公电脑或高权限跳板机共用。

服务端建议配置：

- CPU：4 核以上；
- 内存：16GB；
- 系统盘：SSD；
- 临时上传盘：独立磁盘或分区；
- 正式备份盘：独立磁盘、磁盘阵列或 NAS 后端；
- 网络：千兆以上；
- UPS；
- 固定 IP；
- 禁止睡眠和休眠。

### 6.3 推荐磁盘结构

```text
C:\
 ├─ Windows
 └─ BackupPlatform
    ├─ Server
    ├─ Web
    └─ Config

D:\
 ├─ BackupStaging
 ├─ Database
 └─ Logs

E:\
 └─ BackupRepository
```

正式备份不得与系统盘混合存放。

---

## 7. 系统模块划分

### 7.1 客户端 Agent

客户端包含以下内部模块：

#### 7.1.1 注册与身份模块

负责：

- 首次注册；
- 客户端唯一编号生成；
- 注册令牌校验；
- 客户端证书获取；
- 证书本地安全保存；
- 证书更新和吊销处理。

#### 7.1.2 心跳模块

定期向服务端上报：

- 在线状态；
- 服务器名称；
- 操作系统版本；
- IP 地址；
- Agent 版本；
- Agent 启动时间；
- 系统运行时间；
- 当前配置版本；
- 当前任务状态；
- 客户端本地时间。

默认心跳周期建议为 60 秒。

#### 7.1.3 主机状态采集模块

采集与备份运行相关的基础指标：

- CPU 使用率；
- 内存使用率；
- 可用内存；
- 系统盘剩余空间；
- 备份源磁盘剩余空间；
- Agent 自身 CPU 和内存占用；
- 网卡当前发送和接收速率；
- 指定 Windows 服务状态。

系统不采集全部性能计数器，避免演变成完整监控平台。

#### 7.1.4 配置同步模块

客户端通过心跳检查服务端配置版本。

当服务端配置版本变化时，客户端拉取最新配置，并保存到本地缓存。

客户端离线期间仍可读取上一次有效配置。

#### 7.1.5 目录扫描模块

负责：

- 按任务扫描指定目录；
- 文件过滤；
- 目录过滤；
- 子目录递归；
- 文件时间识别；
- 文件大小识别；
- 文件组合识别；
- 排除临时文件；
- 处理长路径和中文路径。

#### 7.1.6 备份识别模块

第一版支持四种识别器：

##### 最新单文件

适用于：

- `.bak`；
- `.zip`；
- `.rar`；
- `.db`；
- 单一备份文件。

##### 最新目录

适用于每次备份生成一个完整目录的应用。

##### 多文件备份集

根据以下条件组合为一份备份：

- 相同文件名前缀；
- 相同编号；
- 相同日期时间；
- 指定必需文件；
- 正则表达式提取的批次号。

##### 多子目录备份

适用于用友等场景：

- 根目录下每个一级子目录代表一个账套；
- 每个账套分别寻找最新完整备份；
- 一个账套失败不影响其他账套；
- 服务端分别显示每个账套状态。

#### 7.1.7 完整性判断模块

发现最新文件后不能立即上传。

完整性判断过程：

1. 检查最后修改时间是否超过安全等待期；
2. 第一次记录文件数量、大小和修改时间；
3. 等待配置的稳定间隔；
4. 第二次重新扫描；
5. 判断文件数量、大小和时间是否保持不变；
6. 检查必需文件是否齐全；
7. 尝试打开文件确认没有持续写入；
8. 判断容量是否处于合理区间；
9. 生成候选备份集。

建议默认稳定间隔为 10 分钟，可按任务修改。

#### 7.1.8 哈希模块

对候选备份集生成：

- SHA-256；
- 文件总数；
- 文件总容量；
- 相对路径清单；
- 修改时间；
- 备份集唯一标识。

大文件应使用流式读取，避免一次性加载到内存。

#### 7.1.9 指令执行模块

客户端只接受白名单指令：

- 预检；
- 上传最新备份；
- 上传指定候选备份；
- 暂停；
- 继续；
- 取消当前任务；
- 重新扫描；
- 重新校验；
- 同步配置；
- 获取运行状态。

禁止下发：

- 任意 CMD；
- 任意 PowerShell；
- 任意程序启动；
- 任意文件删除；
- 任意注册表操作；
- 任意服务操作。

#### 7.1.10 上传模块

负责：

- 主动连接服务端；
- 分块上传；
- 断点续传；
- 带宽限制；
- 上传重试；
- 网络异常恢复；
- 上传进度上报；
- 文件变化二次校验。

#### 7.1.11 本地状态模块

建议使用 SQLite 或轻量本地文件保存：

- 客户端身份；
- 配置版本；
- 候选备份集；
- 已执行指令；
- 上传断点；
- 待上报事件；
- 最近执行结果。

本地数据库不得保存服务端管理员密码。

---

## 8. 服务端模块

### 8.1 客户端管理

负责：

- 客户端注册；
- 客户端审批；
- 客户端分组；
- 在线状态；
- Agent 版本；
- 证书状态；
- 最后心跳；
- 禁用和注销客户端。

### 8.2 任务配置

每个备份任务包含：

- 所属客户端；
- 任务名称；
- 应用名称；
- 来源路径；
- 识别类型；
- 文件包含规则；
- 文件排除规则；
- 是否递归；
- 账套目录层级；
- 必需文件；
- 文件稳定时间；
- 最小合理容量；
- 最大合理容量；
- 上传模式；
- 调度时间；
- 上传窗口；
- 带宽限制；
- 保留策略；
- 告警策略。

### 8.3 上传模式

每个任务支持以下模式：

#### 自动上传模式

到达计划时间后自动预检，预检通过后自动上传。

#### 人工确认模式

客户端自动扫描和预检，管理员在服务端确认后上传。

#### 完全手动模式

只有管理员下发预检或上传命令后才开始执行。

#### 仅监控模式

只检查应用是否产生新备份，不上传文件。

#### 暂停模式

保留任务配置，但不扫描、不上传。

推荐默认模式：

> 自动扫描、自动预检、每周人工批量下发上传。

### 8.4 指令队列

服务端将管理员操作转换为结构化指令。

每条指令包含：

- 指令 ID；
- 客户端 ID；
- 任务 ID；
- 指令类型；
- 创建时间；
- 失效时间；
- 随机数；
- 操作人；
- 参数；
- 状态；
- 服务端签名。

客户端通过心跳主动领取指令。

同一指令重复领取不得重复执行。

### 8.5 调度模块

调度模块负责：

- 自动任务触发；
- 手动批量任务排队；
- 服务器分组错峰；
- 最大并发控制；
- 单客户端任务控制；
- 带宽控制；
- 磁盘写入压力控制；
- 失败重试；
- 任务超时；
- 优先级管理。

建议默认：

- 全局同时上传 2 台服务器；
- 每台客户端同时上传 1 个任务；
- 同一任务同时只允许 1 个实例；
- 大文件任务和小文件任务可设置不同优先级。

### 8.6 上传接收模块

接收流程：

```text
客户端申请上传
    ↓
服务端验证客户端证书
    ↓
验证任务和备份集
    ↓
检查当前并发与磁盘空间
    ↓
生成上传会话
    ↓
分块接收至临时区
    ↓
记录每块完成状态
    ↓
全部文件接收完成
    ↓
服务端重新计算SHA-256
    ↓
与客户端清单比对
    ↓
校验通过后正式入库
```

上传未完成时，文件只能保存在临时区。

### 8.7 文件校验模块

服务端不能直接相信客户端上报的哈希。

必须重新计算并检查：

- 文件数量；
- 相对路径；
- 单文件大小；
- 单文件 SHA-256；
- 总容量；
- 备份集清单。

只有全部一致，才可标记为“正式入库”。

### 8.8 存储管理模块

负责：

- 临时目录管理；
- 正式目录生成；
- 文件原子移动；
- 重复备份判断；
- 保留策略；
- 过期版本清理；
- 空间预警；
- 仓库定期校验。

客户端无权删除正式备份。

### 8.9 监控模块

展示：

- 客户端在线状态；
- CPU；
- 内存；
- 磁盘；
- Agent 资源占用；
- 最近心跳；
- 关键服务状态；
- 备份发现情况；
- 待上传容量；
- 上传速度；
- 当前进度。

监控数据用于备份运行判断，不建设复杂性能分析系统。

### 8.10 告警模块

第一版支持：

- 管理控制台告警；
- 邮件；
- 企业微信机器人；
- 可选钉钉机器人。

告警类型包括：

- 客户端离线；
- Agent 服务异常；
- 本周未发现新备份；
- 备份文件长期未稳定；
- 某账套未生成备份；
- 上传失败；
- 哈希校验失败；
- 备份容量异常；
- 源磁盘空间不足；
- 服务端空间不足；
- 备份任务长期未人工确认；
- 客户端证书即将过期。

### 8.11 审计模块

记录以下操作：

- 管理员登录；
- 配置新增、修改、删除；
- 手动下发任务；
- 暂停和取消任务；
- 下载备份；
- 删除或清理备份；
- 客户端注册和注销；
- 权限变更；
- 告警处理。

审计日志不允许普通管理员修改。

---

## 9. 备份任务状态机

每个任务应使用明确状态，而不是只有成功和失败。

```text
未扫描
    ↓
正在扫描
    ↓
发现候选备份
    ↓
等待稳定
    ↓
预检通过
    ↓
等待人工确认
    ↓
等待上传许可
    ↓
正在计算哈希
    ↓
正在上传
    ↓
服务端校验
    ↓
正式入库
    ↓
完成
```

异常状态包括：

- 本周无新备份；
- 文件仍在变化；
- 必需文件缺失；
- 容量异常；
- 备份集已变化；
- 上传中断；
- 等待重试；
- 服务端拒绝；
- 校验失败；
- 空间不足；
- 已取消；
- 已过期；
- 客户端离线。

---

## 10. 人工批量上传流程

推荐每周操作流程：

```text
管理员进入控制台
    ↓
点击“本周全部预检”
    ↓
服务端向客户端下发预检指令
    ↓
客户端识别最新完整备份
    ↓
服务端汇总正常、缺失和异常任务
    ↓
管理员确认
    ↓
点击“上传已就绪备份”
    ↓
服务端按优先级和并发排队
    ↓
客户端依次主动上传
    ↓
服务端校验并正式入库
    ↓
形成本周备份报告
```

批量操作必须支持：

- 全选；
- 按服务器组选择；
- 按应用类型选择；
- 排除异常项；
- 设置本次最大并发；
- 设置临时带宽限制；
- 失败任务重新执行。

---

## 11. 备份识别规则

### 11.1 单文件示例

```text
目录：D:\DatabaseBackup
包含：*.bak
排除：*.tmp
选择：最后修改时间最新
稳定时间：10分钟
最小容量：500MB
```

### 11.2 目录型示例

```text
目录：E:\ApplicationBackup
选择：最新一级子目录
完整条件：
- 目录内至少10个文件
- 必须包含backup.info
- 连续两次扫描容量不变化
```

### 11.3 多文件备份集示例

```text
文件：
ERP_20260731_DATA.bak
ERP_20260731_LOG.bak
ERP_20260731_CONFIG.zip

组合条件：
- 日期编号相同
- DATA、LOG、CONFIG三类文件必须齐全
```

### 11.4 用友多账套示例

```text
根目录：D:\UFBackup

D:\UFBackup\001\
D:\UFBackup\002\
D:\UFBackup\003\

规则：
- 一级子目录代表账套
- 每个账套独立寻找最新目录
- 每个账套独立校验
- 账套名称可配置别名
- 任一账套异常时单独告警
```

---

## 12. 存储目录规范

建议正式仓库采用以下结构：

```text
E:\BackupRepository\
└─ ERP-SERVER-01\
   └─ 用友ERP\
      ├─ 账套001\
      │  └─ 2026-07-31_020500\
      │     ├─ 原始备份文件
      │     └─ manifest.json
      └─ 账套002\
         └─ 2026-07-31_021000\
            ├─ 原始备份文件
            └─ manifest.json
```

`manifest.json` 至少记录：

```json
{
  "backupSetId": "ERP01-U8-001-20260731-020500",
  "clientId": "ERP-SERVER-01",
  "taskName": "用友ERP账套备份",
  "businessUnit": "账套001",
  "sourcePath": "D:\\UFBackup\\001",
  "discoveredAt": "2026-07-31T02:20:00",
  "uploadedAt": "2026-07-31T04:10:00",
  "verifiedAt": "2026-07-31T04:20:00",
  "fileCount": 12,
  "totalBytes": 18700000000,
  "files": [
    {
      "path": "backup001.bak",
      "size": 18000000000,
      "lastModified": "2026-07-31T01:30:00",
      "sha256": "..."
    }
  ]
}
```

---

## 13. 数据库设计

核心数据表建议包括：

### 13.1 客户端表

记录：

- 客户端 ID；
- 服务器名称；
- IP；
- 操作系统；
- Agent 版本；
- 在线状态；
- 最后心跳；
- 证书指纹；
- 所属分组；
- 启用状态。

### 13.2 备份任务表

记录：

- 任务 ID；
- 客户端 ID；
- 任务名称；
- 来源目录；
- 识别类型；
- 识别规则；
- 上传模式；
- 时间窗口；
- 稳定时间；
- 带宽限制；
- 保留策略。

### 13.3 候选备份集表

记录：

- 备份集 ID；
- 任务 ID；
- 账套或业务单元；
- 来源路径；
- 发现时间；
- 文件数量；
- 总容量；
- 完整性状态；
- 是否已上传。

### 13.4 上传会话表

记录：

- 上传会话 ID；
- 备份集 ID；
- 开始时间；
- 当前状态；
- 已上传字节数；
- 总字节数；
- 重试次数；
- 最近错误。

### 13.5 文件清单表

记录：

- 文件相对路径；
- 文件大小；
- 修改时间；
- 源端哈希；
- 服务端哈希；
- 上传状态。

### 13.6 指令表

记录：

- 指令 ID；
- 客户端 ID；
- 指令类型；
- 创建人；
- 创建时间；
- 过期时间；
- 状态；
- 执行结果。

### 13.7 心跳状态表

记录客户端最新状态。

历史监控数据只保留必要周期，不保存无限分钟级数据。

### 13.8 告警表

记录：

- 告警类型；
- 告警等级；
- 客户端；
- 任务；
- 发生时间；
- 恢复时间；
- 处理状态；
- 处理人。

### 13.9 审计日志表

记录所有管理操作和敏感操作。

---

## 14. 通信架构

### 14.1 通信方向

只允许：

```text
业务服务器 Agent → 备份服务器
```

服务端不主动建立到业务服务器的入站连接。

### 14.2 网络端口

建议：

- TCP 443：客户端心跳、指令领取、上传；
- TCP 8443：管理控制台；
- 数据库端口仅允许本机访问。

### 14.3 通信协议

采用：

- HTTPS；
- TLS 1.2 及以上；
- 客户端证书认证；
- 服务端证书校验；
- JSON 控制接口；
- 流式文件上传接口。

### 14.4 指令安全

每条指令应具备：

- 唯一 ID；
- 客户端绑定；
- 任务绑定；
- 创建时间；
- 过期时间；
- 随机数；
- 服务端签名；
- 幂等性。

客户端必须拒绝：

- 过期指令；
- 重复指令；
- 目标不匹配指令；
- 未知指令；
- 签名错误指令。

---

## 15. 安全架构

### 15.1 网络安全

建议备份机放置在独立备份网段或 VLAN。

业务服务器只允许访问：

```text
备份服务器IP:443
```

管理控制台只允许管理网段访问。

禁止业务服务器直接访问正式备份目录。

### 15.2 身份认证

每台客户端使用独立身份和独立证书。

不允许全部客户端共用一个账号密码。

单台客户端证书泄露时，可以单独吊销，不影响其他服务器。

### 15.3 服务账号

客户端 Agent 使用低权限服务账号运行。

服务端至少拆分：

- 服务运行账号；
- 平台管理员账号；
- Windows 系统维护账号。

服务不得使用域管理员运行。

### 15.4 存储权限

客户端只能上传，不能：

- 浏览其他服务器数据；
- 覆盖正式文件；
- 修改旧备份；
- 删除历史备份；
- 指定服务端任意保存路径。

### 15.5 管理权限

建议角色：

#### 系统管理员

管理系统配置、客户端、账号和存储。

#### 备份管理员

管理任务、下发上传、查看结果。

#### 审计人员

只读查看日志、记录和告警。

#### 恢复操作员

查询和下载备份，但不能修改任务。

### 15.6 防勒索设计

核心措施：

1. 不使用所有服务器可写的公共 SMB 共享；
2. 客户端只能新增数据；
3. 已归档目录不可由客户端修改；
4. 服务端正式仓库不映射给业务服务器；
5. 重要数据另保留离线或不可变副本；
6. 删除操作必须审计；
7. 清理任务只由服务端保留策略执行。

---

## 16. 断点续传设计

大文件采用分块上传。

建议分块大小：

- 默认 8MB；
- 可根据网络情况配置 4MB 至 32MB。

每块记录：

- 文件 ID；
- 分块序号；
- 起始偏移；
- 分块大小；
- 分块哈希；
- 上传状态。

网络中断后：

1. 客户端重新连接；
2. 查询服务端已接收分块；
3. 从未完成分块继续；
4. 不重复发送已确认分块；
5. 文件完成后计算整体哈希。

---

## 17. 资源感知调度

客户端收到上传指令后，可先检查资源。

建议默认规则：

- CPU 连续高于 85%：暂缓；
- 内存使用超过 90%：暂缓；
- 源磁盘剩余低于 10%：告警；
- 服务端备份盘剩余低于 15%：停止新任务；
- 当前网络流量过高：降低上传速度；
- 业务工作时间：自动降低带宽。

管理员可以选择：

- 严格执行资源限制；
- 只告警但继续；
- 本次强制执行。

---

## 18. 告警等级

### 严重

- 服务端正式备份盘空间不足；
- 哈希校验失败；
- 多个客户端同时异常；
- 正式仓库不可写；
- 数据库不可用；
- 备份文件被异常修改。

### 重要

- 本周未生成新备份；
- 某账套未备份；
- 客户端离线；
- 上传连续失败；
- 候选备份集缺少必要文件。

### 提醒

- 等待人工确认；
- 客户端版本较旧；
- 证书即将到期；
- 上传速度较慢；
- 磁盘空间逐渐不足。

---

## 19. 保留策略

不建议只保留最新一份。

默认可以采用：

- 最近 8 个周版本；
- 最近 6 个自然月的月末版本；
- 最近 2 个年度版本。

不同任务可独立设置。

清理流程：

1. 服务端生成待清理清单；
2. 根据保留策略判断；
3. 记录审计日志；
4. 移入隔离回收区；
5. 保留一定天数；
6. 最终物理删除。

客户端无权触发清理。

---

## 20. 恢复设计

第一版提供：

- 按服务器查询；
- 按应用查询；
- 按账套查询；
- 按日期查询；
- 查看文件清单；
- 查看哈希；
- 下载单文件；
- 下载完整备份集；
- 下载前重新校验。

默认禁止直接覆盖生产目录。

恢复操作必须记录：

- 操作人；
- 下载时间；
- 服务器；
- 应用；
- 备份版本；
- 下载文件；
- 下载结果。

后续可增加：

- ZIP 自动完整性测试；
- SQL Server `RESTORE VERIFYONLY`；
- 定期抽样恢复；
- 测试环境自动恢复验证。

---

## 21. 管理控制台设计

### 21.1 首页总览

显示：

- 客户端总数；
- 在线数量；
- 离线数量；
- 本周已完成任务；
- 等待人工确认；
- 异常任务；
- 备份服务器剩余容量；
- 最近严重告警。

### 21.2 服务器列表

显示：

- 服务器名称；
- IP；
- 在线状态；
- CPU；
- 内存；
- 磁盘；
- Agent 版本；
- 最后心跳；
- 本周备份状态。

### 21.3 服务器详情

显示：

- 基本信息；
- 资源状态；
- 关键服务；
- 所属任务；
- 最近执行记录；
- 最近告警；
- 客户端日志。

### 21.4 任务管理

支持：

- 新增；
- 编辑；
- 复制；
- 启用；
- 暂停；
- 预检；
- 立即上传；
- 查看识别结果。

### 21.5 操作中心

提供：

- 本周全部预检；
- 上传已就绪任务；
- 批量暂停；
- 失败重试；
- 调整本次并发；
- 调整临时带宽。

### 21.6 备份记录

支持按：

- 服务器；
- 应用；
- 账套；
- 日期；
- 状态；

进行筛选。

### 21.7 告警中心

支持：

- 未处理；
- 已确认；
- 已恢复；
- 已忽略；
- 按严重程度筛选。

### 21.8 系统设置

包括：

- 存储目录；
- 全局并发；
- 邮件和企业微信；
- 证书；
- 用户和角色；
- 日志保留；
- 默认保留策略。

---

## 22. 客户端资源控制目标

空闲状态建议目标：

- CPU：接近 0%；
- 内存：50MB 至 100MB；
- 网络：仅少量心跳；
- 磁盘：少量状态和日志。

扫描状态：

- 分批枚举文件；
- 不一次性加载全部目录；
- 可配置扫描深度；
- 避免高频重复扫描。

上传状态：

- 流式读取；
- 限制单任务线程数；
- 默认一个文件顺序上传；
- 哈希和上传可流水线处理；
- 可配置带宽上限。

---

## 23. 服务端可靠性

服务端应具备：

- Windows Service 自动启动；
- 服务异常自动恢复；
- 上传临时文件清理；
- 数据库定期备份；
- 配置定期导出；
- 日志滚动；
- 存储空间检查；
- 服务启动自检；
- 数据库与仓库一致性检查；
- 未完成会话恢复。

服务端重启后：

- 已完成文件不得重新上传；
- 未完成任务自动恢复；
- 临时文件根据会话状态保留；
- 孤立临时文件进入清理队列。

---

## 24. 高可用与灾难恢复

第一版不建设双机集群，但应保证可迁移。

需要定期备份：

- 服务端数据库；
- 服务端配置；
- 客户端证书信息；
- 任务模板；
- 审计日志；
- 正式备份仓库。

恢复新服务端时：

1. 安装服务端程序；
2. 恢复数据库；
3. 恢复配置；
4. 挂载正式备份仓库；
5. 验证客户端证书；
6. 客户端重新连接；
7. 执行仓库一致性检查。

备份文件本身不依赖数据库才能使用。

---

## 25. 技术选型建议

### 客户端

- 语言：C#；
- 运行方式：Windows Service；
- 兼容目标：Windows Server 2012 R2 及以上；
- 本地状态：SQLite；
- 日志：结构化滚动日志；
- 通信：HTTPS；
- 哈希：SHA-256。

### 服务端

- 后端：ASP.NET Core；
- 部署方式：Windows Service；
- 管理界面：Vue 3 或 React；
- 数据库：PostgreSQL、SQL Server Express 或正式 SQL Server；
- 第一版小规模试点可使用 SQLite；
- 文件存储：NTFS 或经过验证的 ReFS；
- 通信：HTTPS + 客户端证书；
- 日志：结构化文件日志 + Windows 事件日志。

### 架构形式

采用模块化单体：

```text
BackupServer
├─ ClientCommunication
├─ TaskManagement
├─ CommandQueue
├─ Scheduler
├─ UploadReceiver
├─ FileVerification
├─ RepositoryManagement
├─ Monitoring
├─ Alerting
├─ Audit
└─ WebApi
```

不拆微服务。

---

## 26. 第一版功能范围

第一版必须完成：

1. 客户端安装和注册；
2. 客户端心跳；
3. CPU、内存、磁盘基础状态；
4. 单文件识别；
5. 最新目录识别；
6. 多文件备份集识别；
7. 多子目录账套识别；
8. 文件稳定性判断；
9. 自动预检；
10. 人工批量下发；
11. 分块上传；
12. 断点续传；
13. SHA-256 校验；
14. 临时区和正式区；
15. 原始目录存储；
16. 版本保留；
17. 失败重试；
18. 告警；
19. 操作审计；
20. 备份记录查询和下载。

---

## 27. 第二阶段功能

后续增加：

- 用友专用识别模板；
- SQL Server 备份验证；
- ZIP、RAR 完整性验证；
- Agent 静默升级；
- 企业微信审批式上传；
- 离线副本登记；
- 存储容量预测；
- 备份容量异常分析；
- 定期抽样恢复；
- 多备份服务器；
- 异地复制；
- 不可变存储；
- Prometheus 指标导出。

---

## 28. 验收标准

第一版上线前至少完成以下测试：

### 功能测试

- 四类备份识别全部通过；
- 用友多账套可分别识别；
- 人工预检和上传正常；
- 自动任务正常；
- 暂停、继续和取消正常。

### 可靠性测试

- 上传中断后可继续；
- 客户端重启后可恢复；
- 服务端重启后可恢复；
- 同一任务重复下发不重复入库；
- 文件变化时能够阻止错误上传；
- 大文件哈希一致。

### 安全测试

- 客户端不能浏览其他备份；
- 客户端不能删除历史备份；
- 非法证书不能连接；
- 过期指令不能执行；
- 重放指令不能重复执行；
- 路径穿越攻击无效；
- 上传文件不能覆盖任意服务端路径。

### 性能测试

- 客户端空闲资源占用符合目标；
- 大目录扫描不会明显影响业务；
- 多客户端同时上传时服务端稳定；
- 带宽限制有效；
- 断点续传有效。

### 恢复测试

- 能查询备份；
- 能下载完整备份集；
- 下载文件哈希正确；
- 用友或数据库测试备份能够恢复。

---

## 29. 项目核心边界

系统最终应始终遵循以下边界：

> 客户端只负责读取本地已经生成的应用备份，并主动上传。

> 服务端只负责集中编排、接收、校验、留存、监控和恢复。

> 系统不获得业务服务器远程管理能力。

> 系统不替代应用自身备份。

> 系统不追求大而全，只解决中小企业应用备份集中管理问题。

---

## 30. 总结

本系统采用轻量 Windows Agent 和集中服务端架构。

其核心链路为：

```text
应用自动生成备份
        ↓
客户端发现并判断完整性
        ↓
客户端上报候选备份
        ↓
管理员确认或自动批准
        ↓
服务端发放上传许可
        ↓
客户端主动分块上传
        ↓
服务端重新校验
        ↓
原始文件正式入库
        ↓
形成记录、告警和恢复入口
```

该架构能够在不改变现有应用备份方式的前提下，降低人工工作量，提高备份完整性、安全性、可追溯性和恢复能力。

系统硬件可以使用普通专用 Windows 电脑，但服务角色必须独立；软件功能可以轻量，但文件校验、权限隔离、断点续传和审计机制不能简化。
