# 系统审查整改追踪

对照 [系统审查.md](./系统审查.md) 的 24 项缺陷逐条落实。

- 验证基线：`dotnet build` **0 警告 0 错误**；测试 **140/140 通过**
  （本地 PostgreSQL 16，`BACKUPMONITOR_TEST_LOCAL_POSTGRES=1`；整改前为 59 项）
- 新增迁移：V011 / V012 / V013
- P0 三项的详细成因与修法另见 [REVIEW-2026-08-21.md](./REVIEW-2026-08-21.md)

---

## 一、24 项缺陷落实情况

### 阻断级（3/3）

| 项 | 问题 | 状态 | 关键改动 | 测试 |
| --- | --- | --- | --- | --- |
| P0-1 | 强制改密只在浏览器里生效 | ✅ | JWT 加 `must_change_password` claim；新增 `PasswordChangeRequiredMiddleware`，挂在认证之后授权之前（同时覆盖 MVC 与 minimal API）；前端改以 `/auth/me` 为权威 | `PasswordChangeGateTests` ×3 |
| P0-2 | LAN Turnkey mTLS 握手失败 | ✅ | `ClientCertificateValidation` 显式放行链校验，身份判定交还指纹查表 | ⚠️ 需实机 |
| P0-3 | 保留策略物理删除无仓库根围栏 | ✅ | 新增 `PathSafety.IsUnderBase`；删除前过围栏，越界则拒绝+告警+留回收站 | `PathSafetyTests`、保留用例四 |

### 高危（8/8）

| 项 | 问题 | 状态 | 关键改动 | 测试 |
| --- | --- | --- | --- | --- |
| P1-1 | 上传完整性校验可被客户端跳过 | ✅ | `ExpectedSha256` 升为**强制**比对基准，客户端声明值降为附加交叉验证；缺预检哈希直接拒绝建会话；补分块长度校验（堵住稀疏空洞） | ⚠️ 无专项 |
| P1-2 | 一个合法策略即可删光某任务全部备份 | ✅ | DTO 加 `IValidatableObject` 跨字段校验（至少一条 Keep\*Count）+ 两个天数下限提到 1；清理器加**回收熔断**（单任务单轮比例超阈值即整体暂停并告严重警，V013 可配） | `回收比例超阈值触发熔断` |
| P1-3 | 表格排序整条链路是死的 | ✅ | 新增 `SortWhitelist`（显式白名单 + 强类型委托，不用反射）；接入客户端/任务/备份集/告警四个列表；前端补发 `sortBy` 并标记 13 个可排序列 | ⚠️ 无专项 |
| P1-4 | 最重要的行动按钮没有样式 | ✅ | 按钮样式与元素解耦：`button` 与 `.button` 共用外观；CI 加 CSS 类引用检查后又查出 3 个同类问题一并补齐 | CI 检查 |
| P1-5 | 命令面板搜索用错查询参数 | ✅ | `applicationName` → `keyword`；加 200ms 防抖；pageSize 200 → 20 并去掉前端二次过滤 | — |
| P1-6 | manifest 落盘同步无效 | ✅ | 只读流 `Flush(true)` 改为 `FileOptions.WriteThrough` 写模式；分块写入同样补 WriteThrough | ⚠️ 无专项 |
| P1-7 | 下载单文件即标记整个恢复请求完成 | ✅ | V012 加 `delivered_paths`，按**已交付文件集合**判定完成；单文件分支补齐未开始写出时的重抛 | ⚠️ 无专项 |
| P1-8 | 暂存空间检测失败时 fail-open | ✅ | 异常路径由 `long.MaxValue` 改为 `0`，与「驱动器未就绪」口径一致，fail-closed | — |

### 中低（13/13）

| 项 | 问题 | 状态 | 关键改动 |
| --- | --- | --- | --- |
| P2-1 | 账号停用状态在验密前泄露 | ✅ | 移除前置停用检查，状态校验统一放到密码校验通过之后，消除用户名枚举 |
| P2-2 | 登录无 IP 级限速 | ✅ | 接入 `AddRateLimiter`，登录/刷新按对端 IP 固定窗口 10 次/分钟，429 返回统一错误体 |
| P2-3 | 恢复下载令牌出现在 URL 路径中 | ⚠️ 缓解 | 切断实际泄露路径：Serilog 请求日志对该路径打码，nginx 对 `/api/v1/downloads/` 关闭 access_log。**协议本身仍在路径携带令牌**——改请求头需要一整套一次性会话交换，属独立接口变更 |
| P2-4 | 刷新 Cookie 有效期与令牌真实 TTL 不一致 | ✅ | 签发方把实际 TTL 随响应返回，Cookie `MaxAge` 取同一来源，不再各自读配置 |
| P2-5 | 弱口令表是象征性的 | ✅ 缓解 | 补**结构性**弱口令判定（重复字符/纯数字/键盘连续序列），比枚举字典更有效；并在注释里写明这不是完整字典防线及后续接入方式 |
| P2-6 | PathSafety 未拦 Windows 保留设备名 | ✅ | `CON/PRN/AUX/NUL/COM1-9/LPT1-9`（含带扩展名）一律拒绝；服务端生成路径撞名时前缀改写 |
| P2-7 | 备份集编码会在删除后被复用 | ✅ | V011 引入序列 `backup_set_code_seq`，单调递增不回落；迁移时推进到现存最大序号之后 |
| P2-8 | 清单与数据库记录的哈希来源不同 | ✅ | manifest 改用 `ServerSha256 ?? ExpectedSha256`，与 `BackupFile.Sha256` 同源 |
| P2-9 | 原始文件修改时间在入库时被丢弃 | ✅ | `BackupFile.LastModifiedAt` 改取 `CandidateFile.LastModifiedAt` |
| P2-10 | 同一文件的分块无法并发上传 | ✅ | `FileShare.Read` → `ReadWrite`，`useAsync:false` → `FileOptions.Asynchronous`；各分块写入区间由 offset 严格划分不重叠 |
| P2-11 | confirmModal 被 Esc 关闭时 Promise 永不 resolve | ✅ | `closeModal` 派发 `bm:dismiss`，任何关闭途径都让 Promise 落定为「取消」 |
| P2-12 | 待办页计数不含自动登记条目 | ✅ | 计数并入 `recentAutoEnrollments.length` |
| P2-13 | 零散一致性问题（5 小项） | ✅ | 部署状态卡走 `fmtDT`；空态标题去掉双重转义；筛选 chip 统一 `<strong>`（恢复等宽数字）；`nth-child(-n+1)` 遗留写法清理；`font-weight:700` → 600 |

---

## 二、审查清单之外新发现的缺陷

### 🔴 告警系统从未产生过任何告警

`AlertingService.RaiseAsync` 的去重前置守卫判断方向写反：

```csharp
var hasActiveAlert = await _db.Alerts.AnyAsync(a => a.AlertKey == alertKey && ActiveStatuses.Contains(a.Status), ct);
if (!hasActiveAlert)
    return;   // ← 「不存在活动告警」恰恰是需要新建的情形
```

结果是下面创建新告警的 `else` 分支成为**死代码**，全系统任何 `RaiseAsync` 都不产生告警——
入库失败、保留删除失败、恢复校验失败、回收熔断，监控的输出端整体失效。

这一项严重度不低于原报告的 P1，且完美印证了报告 §06 的判断：
告警「产生」环节此前**零测试覆盖**，已有测试只覆盖投递环节，所以它能一直存活。
发现过程也正是这样——新写的熔断测试查不到告警，回溯才找到根因。

**修复**：去掉方向写反且与下方 `existing` 查询重复的前置守卫。
**测试**：新增 `AlertingServiceTests` ×4（首次必须新建 / 同键累加不新建 / 等级只升不降 / 恢复后再触发新建）。

### 提交后收尾动作可摧毁已成功的备份

`UploadCommitWorker` 中 `CleanupSessionAsync` 与 `RecoverAsync` 位于事务提交**之后**却仍在同一 try 内，
而对应 catch 会回滚事务并删除正式目录。此前未出事仅因「对已提交事务调 `RollbackAsync` 会先抛异常」
这一实现细节恰好挡在删除动作之前——换个 EF/Npgsql 版本即可能真的删下去。

**修复**：提交后置 `committed = true`，收尾动作各自兜住异常仅记日志；catch 增加 `if (committed) throw;` 闸门。

---

## 三、测试与 CI

| 项 | 状态 | 说明 |
| --- | --- | --- |
| 两个 Setup 项目进 CI | ✅ | `Agent.Setup` 与 `Server.Setup` 加入 Windows job |
| 前端进 CI | ✅ | 新增 `frontend-check` job：ES 模块语法检查 + **CSS 类引用检查**（该检查落地当场就查出 3 个 P1-4 同类问题） |
| 告警产生/去重 | ✅ | 新增 4 项 |
| 保留清理熔断 | ✅ | 新增 1 项 |
| 路径安全（保留设备名） | ✅ | 新增 21 项 |
| 恢复下载 / ZIP / Range | ❌ | 仍无自动化测试 |
| UploadCommitWorker 入库与回滚 | ❌ | 仍无自动化测试 |
| mTLS 客户端证书认证 | ❌ | 需实机 |

---

## 四、仍未处理

按原报告的判断，这些都需要独立决策或实机环境，不适合在本轮一并改掉。

**需要产品决策**
- **恢复审批分离**：创建恢复请求与签发下载令牌可以是同一人，缺第二人复核。这是控制点设计，不是缺陷修复。
- **下载令牌非一次性**：TTL 内可重复使用。若为支持断点续传是有意为之，应显式记录该决策。
- **P2-3 协议形态**：令牌仍在 URL 路径中（已切断日志泄露路径）。改为请求头需要一次性会话交换机制。

**需要新增机制**
- **仓库孤儿目录对帐**：进程在 `Directory.Move` 之后、事务提交之前崩溃，会留下无数据库记录的目录。回滚路径处理了异常但处理不了崩溃，且无任何巡检任务会发现。需要一个对帐任务。

**需要实机环境**
- P0-2 的 mTLS 正向用例，以及原报告「第二步」建议的完整端到端跑通（注册→心跳→扫描→上传→入库→恢复下载）。这一步的信息量高于继续静态审查。

**界面**（原报告「需要改的地方」中较主观的几项）
- 操作列按钮过多不分主次（建议只留主操作、其余收进溢出菜单）
- 告警列表每行两个视觉相同的红色胶囊
- 命令面板缺顶栏入口提示

已完成的界面项：侧栏改为默认展开、`▤` 图标重复已换、趋势折线 `preserveAspectRatio` 已修、`.button` 样式已补。
